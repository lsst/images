Cell-coadd aperture corrections can how have different missing cells (and hence different bounds) for different fields.

This does not change serialization schemas at all, but it will change how serialized cell aperture correction fields with NaNs are interpreted (previously some NaNs would not have been reflected in the bounds).
