Added a ``mypy-coverage`` GitHub Actions workflow that enforces 100% mypy type-annotation coverage for the ``python`` source tree.
Added missing type annotations across the source and test suites (notably ``-> None`` on ``__init__`` and test methods) and removed redundant default settings from the mypy configuration.
