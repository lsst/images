Accept a legacy `lsst.geom.Box2I` as the `bbox` parameter in butler formatters.

This is necessary to allow code using storage class conversion to get a legacy type to work even when using parameters.
